info:
  name: ListarGeneros
  type: http
  seq: 4

http:
  method: GET
  url: localhost:8080/generos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
