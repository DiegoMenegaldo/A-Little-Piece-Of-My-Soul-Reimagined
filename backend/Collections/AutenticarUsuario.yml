info:
  name: AutenticarUsuario
  type: http
  seq: 5

http:
  method: POST
  url: localhost:8080/usuarios/autenticar
  body:
    type: json
    data: |-
      {
        "email": "sean.brennan@lam.com",
        "senha": "SacrificeMyselToYou1991!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
