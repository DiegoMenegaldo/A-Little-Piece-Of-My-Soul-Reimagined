info:
  name: TestarLogin
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/usuarios/autenticar
  body:
    type: json
    data: |-
      {
        "email": "diego@email.com",
        "senha": "Diego123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
